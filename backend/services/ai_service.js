export const recommendFlowers = (occasion, budget, color) => {
  return `For ${occasion}, you can buy ${color} roses within budget ${budget}`;
};

export const generateBouquet = (occasion, budget) => {
  return `Bouquet idea for ${occasion} under ${budget} includes roses, lilies, and tulips`;
};

9385285928;
